import React from 'react';
import { 
    HeaderContainer,
    HeaderGreeting,
} from './headerComponents';

const Header = () => {
    return (
        <HeaderContainer>
            <HeaderGreeting>
                Loading Data
            </HeaderGreeting>
        </HeaderContainer>
    );
};

export default Header;